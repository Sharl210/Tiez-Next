# 维护决策记录

<!-- ============================================================
     这份文件是什么
     ============================================================
     这是本项目的**决策流水账**：把"某个时间点做了一个会影响后续维护的决定"
     逐条追加记录，只增不改。

     它与 `docs/MAINTENANCE-LIABILITIES.md` 是**两份不同的东西**，不要混淆：

     | | 本文件（维护决策记录） | docs/MAINTENANCE-LIABILITIES.md |
     |---|---|---|
     | 组织方式 | 按**时间**追加，只增不改 | 按**主题**分组，可反复重写 |
     | 回答的问题 | 「谁在何时做了什么决定、影响什么、被谁替代」 | 「这段代码为什么不能改」 |
     | 怎么用 | 追溯决策链、检查是否已被后续覆盖 | 改动某模块前查阅 |
     | 适合 | 决策会演变、会被取代的场景 | 知识相对稳定、需要按主题查阅的场景 |

     两者有交集但不重复：本文件记**决策的时间与替代关系**，那份记**动手前的注意事项**。
     同一条决定可能两边都有，各有各的用途。

     ============================================================
     怎么读（为了长文件而设计，不需要从头读）
     ============================================================
     1. 先看下面的**索引**：每条一行，含编号、时间、状态、标题。
     2. 取清单：      grep -n '^## \[' 维护文档.md
     3. 读单条：      sed -n '起,止p' 维护文档.md
     4. 找相关：      按编号搜 `SUPERSEDES` / `关联条目` 字段
     5. **不要整篇读入**：字段按行分列是为了让"只读一条"足够便宜。

     ============================================================
     怎么追加（维护本文件的规则）
     ============================================================
     · **只追加，不删改**既有条目。
     · **修正**不改旧条：新写一条，用 `SUPERSEDES` 指向被修正/取代的编号。
     · **写入时间**取真实时钟，精确到秒并带时区，形如 `2026-09-24 08:34:10 +08:00`。
     · **发生时间**若为回溯，按已知精度如实标注；**不知道就不写秒**，不要编。
     · 每条独立成块，末行是 `<!-- END MD-序号 -->`，便于机器定位边界。
     · 并发写入产生分叉时标 `CONFLICT`，**不要静默覆盖**。

     ============================================================
     字段说明
     ============================================================
     编号         MD-<四位序号>，只增不改；撤销的条目保留编号并标状态
     写入时间     本条被写进本文件的时刻（真实时钟，含时区）
     发生时间     该决定实际发生的时刻（回溯条目按已知精度）
     状态         生效 | 被替代 | 已废止
     类别         架构 | 接口 | 命名 | 数据布局 | 版本依赖 | 环境适配 |
                  签名打包 | 迁移清理 | 兼容约束 | 已知缺口 | 替代覆盖
     触发背景     为什么要做这个决定（现象、事故、需求）
触发背景_动机 该决定的直接动机（补充「触发背景」，可省略）
     决策内容     决定了什么（可执行的事实）
     持续副作用   这个决定会**长期**影响什么 —— 这是本文件的核心字段
     影响范围     涉及的文件/模块/行为
     后续约束     后来的人必须遵守什么
     回滚或替代   怎么撤销、或被哪条取代
     证据与来源   可核对的出处（文件路径、测试名、外部资料）
     关联条目     相关编号
     ============================================================ -->

## 索引

| 编号 | 写入时间 | 状态 | 类别 | 标题 |
|---|---|---|---|---|
| [MD-0001](#md-0001) | 2026-09-24 08:41:00 +08:00 | 生效 | 架构·发布链路 | 应用改名与发布链路重建 |
| [MD-0002](#md-0002) | 2026-09-24 08:41:00 +08:00 | 生效 | 架构·数据布局 | 数据目录与程序目录分离 |
| [MD-0003](#md-0003) | 2026-09-24 08:41:00 +08:00 | 生效 | 签名打包·环境适配 | 卸载器递归删除加护栏 |
| [MD-0004](#md-0004) | 2026-09-24 08:41:00 +08:00 | 生效 | 兼容约束 | 迁移接管判据只看用户数据 |
| [MD-0005](#md-0005) | 2026-09-24 08:41:00 +08:00 | 生效 | 迁移清理·命名 | 旧版卸载注册表项清理 |
| [MD-0006](#md-0006) | 2026-09-24 08:41:00 +08:00 | 生效 | 架构·数据布局 | 备份路径护栏下沉到内核 |
| [MD-0007](#md-0007) | 2026-09-24 08:41:00 +08:00 | 生效 | 兼容约束·接口 | 云同步按前缀排除 AI 接口配置 |
| [MD-0008](#md-0008) | 2026-09-24 08:41:00 +08:00 | 生效 | 数据布局·架构 | 自动容灾备份的存储位置与轮换 |
| [MD-0009](#md-0009) | 2026-09-24 08:41:00 +08:00 | 生效 | 数据布局·接口 | 固定状态双写并集 |
| [MD-0010](#md-0010) | 2026-09-24 08:41:00 +08:00 | 生效 | 接口·兼容约束 | MCP 默认全开且与界面能力对等 |
| [MD-0011](#md-0011) | 2026-09-24 10:40:00 +08:00 | 生效 | 签名打包·兼容约束 | 覆盖升级默认值改为「直接覆盖」 |
| [MD-0012](#md-0012) | 2026-09-24 10:40:00 +08:00 | 生效 | 架构·接口 | 凭据类与「机器专属」类应显式分开 |
| [MD-0013](#md-0013) | 2026-09-24 10:40:00 +08:00 | 生效 | 架构·接口 | 「用了未定义的样式变量」纳入构建拦截 |
| [MD-0014](#md-0014) | 2026-09-24 10:40:00 +08:00 | 生效 | 环境适配·方法论 | 探针与夹具本身也需要「能失败的证明」 |
| [MD-0015](#md-0015) | 2026-09-24 11:10:00 +08:00 | 生效 | 接口·安全 | 安全告知的送达状态必须只由本机决定 |
| [MD-0016](#md-0016) | 2026-09-24 13:40:00 +08:00 | 生效 | 数据布局·文件操作 | 改名让位类操作必须「失败即回滚」 |
| [MD-0017](#md-0017) | 2026-09-24 13:40:00 +08:00 | 生效 | 兼容约束·文件操作 | 「被占用」的判定不能依赖平台错误码 |
| [MD-0018](#md-0018) | 2026-09-24 13:40:00 +08:00 | 生效 | 环境适配·验证方法 | 验证台漏加载样式会让「多主题验证」整体失真 |
| [MD-0019](#md-0019) | 2026-09-24 16:32:00 +08:00 | 生效 | 执行流程·验证方法 | 改名让位与覆盖类操作的文件句柄约束 |
| [MD-0020](#md-0020) | 2026-09-24 16:32:00 +08:00 | 生效 | 执行流程·验证方法 | 平台错误码判定必须走 ErrorKind，且留真机路径的测试 |
| [MD-0021](#md-0021) | 2026-09-24 16:32:00 +08:00 | 生效 | 环境适配·验证方法 | 非目标平台上验证会产生三种假绿 |
| [MD-0022](#md-0022) | 2026-09-24 16:32:00 +08:00 | 生效 | 兼容约束·配置默认值 | 出厂默认值只能有一处权威，且必须与读取端判据一致 |
| [MD-0023](#md-0023) | 2026-09-24 16:32:00 +08:00 | 生效 | 执行流程·变更管理 | 反向对照的回滚步骤本身是最危险的一步 |
| [MD-0024](#md-0024) | 2026-09-24 17:20:00 +08:00 | 生效 | 对外文案·发布流程 | 发布说明面向用户，与 README 的分工是单向派生 |
| [MD-0025](#md-0025) | 2026-09-24 18:30:00 +08:00 | 生效 | 环境适配·验证方法 | 大容器上的 backdrop-filter 会整片压暗内容，与主题无关 |
| [MD-0026](#md-0026) | 2026-09-24 18:32:00 +08:00 | 生效 | 接口·兼容约束 | 备注可编辑性按「正文可编辑类型的补集」判定 |
| [MD-0027](#md-0027) | 2026-09-24 18:34:00 +08:00 | 生效 | 执行流程·验证方法 | 系统级设置清单不得用「已读标记」，探测失败必须与未满足区分 |

---

## [MD-0001]

编号: MD-0001
写入时间: 2026-09-24 08:41:00 +08:00
发生时间: 2026-09 期间（本文件首次建立时回溯收录；发生时刻按当时记录精度，不含精确秒）
状态: 生效
类别: 架构·发布链路
标题: 应用改名与发布链路重建
触发背景: 项目 fork 后改名 Tiez-Next 并重建 Windows 发布链路；上游两个分支形态不同（主分支只有 macOS 打包目标，Windows 配置在另一分支）。
触发背景_动机: Windows 发布链路从上游另一分支移植而来，改名后需要同时处理「必须改」与「必须保留旧名」两类位置。
决策内容: 写进 README 的「相对原版差异」与发布包名；包名/可执行文件名/产品名/窗口标题/多语言应用名/CI 产物名全部改为新名。自启动注册表值名、卸载脚本进程名、旧版卸载注册表项则**保留旧名**（见 MD-0005）。
持续副作用: **上游对这条移植来的发布链路没有维护责任**，其修复不会自动进入本项目。发布链路出问题时，对照点只能是本仓库历史与 Tauri bundler 自身行为。
影响范围: `src-tauri/tauri.conf.json`、`src-tauri/nsis/`、`package.json`、`src-tauri/Cargo.toml`
后续约束: 改名相关问题先查本仓库历史，不要指望上游同步。
回滚或替代: 不适用（改名是单向决定）。
证据与来源: `src-tauri/tauri.conf.json`、`src-tauri/nsis/`；记忆 `AB2-G3-074`
关联条目: MD-0003、MD-0005

<!-- END MD-0001 -->

---

## [MD-0002]

编号: MD-0002
写入时间: 2026-09-24 08:41:00 +08:00
发生时间: 2026-09 期间（本文件首次建立时回溯收录；发生时刻按当时记录精度，不含精确秒）
状态: 生效
类别: 架构·数据布局
标题: 数据目录与程序目录分离
触发背景: 数据目录原先可能落在**程序目录内部**（便携形态 `<exe 同级>/data`），而卸载器会递归删除程序目录，导致卸载即删数据。
触发背景_动机: 实测确认便携判定会**覆盖**用户已显式选择的数据位置；且判定条件仅「目录存在」，不检查是谁创建的。
决策内容: 数据一律放用户目录，程序目录只留可再生的可执行文件；解析优先级＝`datapath.txt 显式重定向` > `用户目录已有数据库则原地沿用` > `用户目录默认位置`。便携判定**整段移除**。
持续副作用: 老用户的 `<exe 同级>/data` 形态**不再被自动识别**。这是有意的：程序目录位置无法被应用可靠推断（用户可能装在任意路径）。旧数据由用户**自己通过迁移中心**手动指定路径迁入。
影响范围: `src-tauri/src/app/setup.rs`（写入侧）、`src-tauri/src/migration_identifier.rs`（读取侧，**保持不动**）
后续约束: 不要为「方便」重新引入「检测程序目录里的数据目录」这类自动推断。迁移中心里**识别**便携形态的逻辑必须保留——那是「认得老数据在哪」，与「新数据写到哪」是两件事。
回滚或替代: 若要恢复便携形态，必须同时解决「卸载不删数据」，否则等于恢复数据丢失风险。
证据与来源: `src-tauri/src/app/setup.rs`；记忆 `AB2-G3-075`
关联条目: MD-0003、MD-0004

<!-- END MD-0002 -->

---

## [MD-0003]

编号: MD-0003
写入时间: 2026-09-24 08:41:00 +08:00
发生时间: 2026-09 期间（本文件首次建立时回溯收录；发生时刻按当时记录精度，不含精确秒）
状态: 生效
类别: 签名打包·环境适配
标题: 卸载器递归删除加护栏
触发背景: 卸载脚本里有一段**无条件递归删除整个安装目录**的逻辑，从上游原样继承。安装版下数据不在安装目录内故无害，但便携形态下数据就在里面，会被一并删除。
触发背景_动机: 这段 hook 来自上游另一分支，随 MD-0001 一并移植进来。
决策内容: 加三条判定：目录非空且长度合理（拒盘根/裸盘符）、含本应用安装标记、**不含数据痕迹**。命中数据痕迹时**保留目录并提示用户**，不静默删除。
持续副作用: 判定必须在**卸载前**执行。安装模板在卸载段里会先删掉主程序与卸载器自身，等卸载后阶段再想确认「这真是我们的安装目录」时，**证据已被自己删掉**——护栏会静默失效（看起来有代码，实际永远判不出身份）。
影响范围: `src-tauri/nsis/uninstall.nsh`
后续约束: 判定时机不能挪到卸载后阶段。改动后必须重跑 `guard-selftest.nsi`（它用真实 NSIS 二进制验证防护行为）。
回滚或替代: 不适用（护栏只增加保护）。
证据与来源: `src-tauri/nsis/uninstall.nsh`、可执行自检 `src-tauri/nsis/guard-selftest.nsi`
关联条目: MD-0002

<!-- END MD-0003 -->

---

## [MD-0004]

编号: MD-0004
写入时间: 2026-09-24 08:41:00 +08:00
发生时间: 2026-09 期间（本文件首次建立时回溯收录；发生时刻按当时记录精度，不含精确秒）
状态: 生效
类别: 兼容约束
标题: 迁移接管判据只看用户数据
触发背景: 判断「新版数据目录能否被旧数据迁移接管」时，曾要求**设置项与全新安装逐键完全相等**。该判据必然失败：应用自身会主动写设置，最平凡的是**窗口尺寸**（拖动窗口即写入）。
触发背景_动机: 用户真机反馈「旧版数据迁不过来、界面说没有数据要迁移」，实测复现并定位到本条。
决策内容: 改为只看有无**用户数据**（剪贴板记录、用户自建标签），不看设置是否还是出厂值。
持续副作用: 原先有一条测试 `customized_setting_makes_target_non_pristine` **断言了这个错误行为**（「用户改过设置就不能算空库」），等于用测试保护了缺陷——测试全绿，功能全坏。该测试已改写为断言正确行为，并补了一条防放宽过头的反向测试。
影响范围: `src-tauri/src/app/commands/system_cmd.rs`
后续约束: 两条测试**互为反向**，一起看才有意义，不要只留一条。判据类代码改动后必须做反向对照（退回旧实现确认测试变红）。
回滚或替代: 若将来「接管」语义变化，需重新定义判据并同步改写这一对测试。
证据与来源: `src-tauri/src/app/commands/system_cmd.rs`；记忆 `AB2-G3-069`
关联条目: MD-0002、MD-0005

<!-- END MD-0004 -->

---

## [MD-0005]

编号: MD-0005
写入时间: 2026-09-24 08:41:00 +08:00
发生时间: 2026-09 期间（本文件首次建立时回溯收录；发生时刻按当时记录精度，不含精确秒）
状态: 生效
类别: 迁移清理·命名
标题: 旧版卸载注册表项清理
触发背景: 旧版（改名前产品名）在系统「添加/删除程序」里留下一条以旧名注册的卸载项，而本项目从未清理——它清理的是**另一种键**。
触发背景_动机: 用户真机反馈「卸载时显示的是原版的名字」。审计确认我们的安装包显示名完全正确，来源是旧版残留条目。
决策内容: 两种键容易混淆：一是「上次安装到哪」（安装路径键），二是「添加/删除程序里的条目」（卸载键，**由产品名拼成**）。改名后卸载键名随之改变，新版安装器读不到旧键→当成全新安装→**新旧两条并列共存**。清理放在**安装前**执行，并在应用启动时也清一遍。
持续副作用: 跨产品名升级时，新版安装器**根本不会运行旧版的卸载器**，所以清理放「卸载时」无效。判据必须**显式排除自己**，不能依赖「新名恰好不含旧名」这种大小写巧合。
影响范围: `src-tauri/nsis/uninstall.nsh`（安装前）、`src-tauri/src/migration.rs`（启动时）
后续约束: 清理路径要读写注册表、往往是平台条件编译的，测不动——因此把**判定**抽成纯函数并加测试（判定才是最容易错的地方）。
回滚或替代: 当可确定不会再有人从旧产品名覆盖安装时（旧版本停止分发足够久）。在此之前移除会让老用户机器残留清理不掉。
证据与来源: `src-tauri/src/migration.rs`；记忆 `AB2-G3-080`
关联条目: MD-0001

<!-- END MD-0005 -->

---

## [MD-0006]

编号: MD-0006
写入时间: 2026-09-24 08:41:00 +08:00
发生时间: 2026-09 期间（本文件首次建立时回溯收录；发生时刻按当时记录精度，不含精确秒）
状态: 生效
类别: 架构·数据布局
标题: 备份路径护栏下沉到内核
触发背景: 「不许把备份导出到数据目录内部」这条检查原先只在**界面**的导出命令里，通过 AI 接口触发导出时**完全绕过**。
触发背景_动机: 安全审计中发现 AI 接口可让应用把备份写到任意路径，含数据目录内部。
决策内容: 把检查下沉到备份内核（所有导出路径的唯一汇聚点）——往数据目录里写备份会**截断正在使用的数据库**（同名文件已存在会先被清空），指向数据库文件即不可逆损毁。
持续副作用: ① 不能把备份（含自动备份）放在数据目录内部；② 新增导出入口时**不需要**再写一遍检查，但**也不要**绕过内核直接写文件。
影响范围: `src-tauri/src/services/backup/export.rs`；AI 接口与界面的全部导出路径
后续约束: 保护性检查要放在唯一汇聚点，不要散落在调用方——散落式的失效模式是「新增调用方忘了加」，且不会有测试失败。
回滚或替代: 不适用（护栏只增加保护）。
证据与来源: `src-tauri/src/services/backup/export.rs`、测试 `create_backup_actually_applies_the_guard`
关联条目: MD-0008

<!-- END MD-0006 -->

---

## [MD-0007]

编号: MD-0007
写入时间: 2026-09-24 08:41:00 +08:00
发生时间: 2026-09 期间（本文件首次建立时回溯收录；发生时刻按当时记录精度，不含精确秒）
状态: 生效
类别: 兼容约束·接口
标题: 云同步按前缀排除 AI 接口配置
触发背景: AI 接口的配置含**访问令牌**与「是否允许写入」「是否允许局域网访问」等**安全姿态开关**，而云同步会把远端设置写回本地并持久化。
触发背景_动机: 安全审计发现一个被篡改的远端快照即可把这些危险配置种进本机，用户从未在本地做过该选择。
决策内容: 按**前缀**整族排除（而非逐键列举）：AI 接口配置、自动容灾备份配置各自整族排除。
持续副作用: 新增该接口的设置项时**不需要**改排除清单（前缀已覆盖）；但**不要**改前缀，也**不要**把某条配置加回同步白名单。逐键列举的失效模式是「新增一个键就静默重新打开这个口子」，且不会有任何编译错误或测试失败——这正是选择前缀的原因。
影响范围: `src-tauri/src/services/cloud_sync.rs`
后续约束: 自动备份的「最大留存份数」若被同步，一个远端快照可把它改成 1，下次轮换会删光本机几乎全部容灾副本。
回滚或替代: 只有当这一族设置里**不再包含任何凭据或安全姿态开关**时才可放宽。
证据与来源: `src-tauri/src/services/cloud_sync.rs` 的 `is_setting_sync_eligible`
关联条目: MD-0008、MD-0010

<!-- END MD-0007 -->

---

## [MD-0008]

编号: MD-0008
写入时间: 2026-09-24 08:41:00 +08:00
发生时间: 2026-09 期间（本文件首次建立时回溯收录；发生时刻按当时记录精度，不含精确秒）
状态: 生效
类别: 数据布局·架构
标题: 自动容灾备份的存储位置与轮换
触发背景: 新增自动容灾备份（定时 + 启动时各一次），与**用户手动导出**是两条不同的链：手动导出由用户选路径、不受份数约束；自动备份由应用保管、参与份数轮换、路径独立。
触发背景_动机: 用户要求加入容灾：定时备份（默认开、30 分钟、最多 20 份、可调 1–200）、启动时备份一次（独立选项、不受定时开关约束）、备份列表可查看/固定/删除/恢复。
决策内容: 存储位置不能是数据目录内部（受 MD-0006 护栏所限），最终选**与数据目录平级的兄弟目录**。轮换：超限时删**最老的未固定**备份，固定的**一律不删**。
持续副作用: 边界行为**已定义，改动时不要重新发明**：最大留存数为 1 时可固定 0 个；全部被固定导致删不动时如实上报「多余份数」并警告，**不静默超限**；最大留存数被调小时**下一次轮换立即生效**（不是只对新备份生效）；目录里的外来文件**不列表、不轮换、不删除**。
影响范围: `src-tauri/src/services/auto_backup/`、设置页与备份列表界面
后续约束: 容灾防的是**逻辑损坏**（误删、误改、导入坏包、升级搞崩），**不防**整盘故障与机器丢失——后者需要用户把备份拷到别处。**不要**因为「更安全」而给它加加密、异地副本、上传远端：那会把「保险」变成「又一个会失败、要权限、要空间的后台任务」。
回滚或替代: 若将来要改变容灾覆盖面（如加异地副本），应新增机制而非改写本条的边界定义。
证据与来源: `src-tauri/src/services/auto_backup/`（`store.rs` / `config.rs` / `schedule.rs`）
关联条目: MD-0006、MD-0007、MD-0009

<!-- END MD-0008 -->

---

## [MD-0009]

编号: MD-0009
写入时间: 2026-09-24 08:41:00 +08:00
发生时间: 2026-09 期间（本文件首次建立时回溯收录；发生时刻按当时记录精度，不含精确秒）
状态: 生效
类别: 数据布局·接口
标题: 固定状态双写并集
触发背景: 「固定某份备份使其不被轮换删除」这个状态需要持久化，而单一存储位置各有失效模式。
触发背景_动机: 设计轮换时需决定固定状态存哪：数据库/索引会与文件分离；仅文件名则改名即丢失。
决策内容: 状态存**两处**：文件名尾部标记 + 目录内索引文件，判定取**并集**。
持续副作用: 只放索引则状态与文件**分离**——用户搬动或复制目录后，索引说「固定」而文件已不在（或反过来），**静默删掉用户固定过的备份**；只放文件名则一改名就无声丢失状态。取并集**只会多保护不会少保护**；索引损坏时可从文件名恢复并给出可读警告。
影响范围: `src-tauri/src/services/auto_backup/store.rs`
后续约束: 取消固定时两处**同时**去掉，任一步失败即回退，杜绝「文件名说有、索引说没有」的分叉。
回滚或替代: 若将来引入更可靠的单一事实源（如带事务的数据库），可简化为单写——但必须先证明迁移无丢失。
证据与来源: `src-tauri/src/services/auto_backup/store.rs`、测试 `pinned_state_survives_a_restart`、`pin_state_falls_back_to_the_file_name_when_the_index_is_gone`
关联条目: MD-0008

<!-- END MD-0009 -->

---

## [MD-0010]

编号: MD-0010
写入时间: 2026-09-24 08:41:00 +08:00
发生时间: 2026-09 期间（本文件首次建立时回溯收录；发生时刻按当时记录精度，不含精确秒）
状态: 生效
类别: 接口·兼容约束
标题: MCP 默认全开且与界面能力对等
触发背景: 内置 AI 接口（MCP）的默认姿态是**开启、全权限、免鉴权、绑定回环**（局域网默认关）。
触发背景_动机: 用户的明确要求；不因「更安全」而收紧默认值。这意味着**能力必须做实**，而不是靠削减默认能力来降风险。
决策内容: 保持默认全开；把能力补齐到与界面操作对等（含读写会话态条目、设置读写、备份导入、剪贴板写入等）。
持续副作用: 审计发现两类真实缺口：① **接口读不到「会话态条目」**——界面呈现的是「数据库 ∪ 内存态未落盘条目」，接口只读数据库；当「持久化存储」关闭（出厂默认）时，**接口看到的条目集合与用户屏幕上的条目集合完全不相交**，而这类缺口在任何「命令清单 vs 工具清单」对账表上**都看不出来**；② 「重置设置」会**静默抹掉接口自身的姿态**，令牌与安全开关被清除后回到出厂默认，且**正在运行的服务保持重置前的姿态**，与库里不一致，重启才暴露。
影响范围: `src-tauri/src/services/mcp/`、`src-tauri/src/app/commands/settings_cmd.rs`
后续约束: 新增设置项时要判断它是否属于该接口的敏感族（见 MD-0007）；破坏性操作必须有显式确认门；刻意不开放的能力（能关掉应用、需提权、改数据位置等）应保持关闭并记录理由。
回滚或替代: 若将来收紧默认姿态，需同步更新本节与用户可见说明。
证据与来源: `src-tauri/src/services/mcp/`；记忆 `AB2-G3-081`
关联条目: MD-0006、MD-0007

<!-- END MD-0010 -->

---

## [MD-0011]

编号: MD-0011
写入时间: 2026-09-24 10:40:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 签名打包·兼容约束
标题: 覆盖升级默认值改为「直接覆盖」
触发背景: 安装包遇到已装版本时会弹一个选择页，模板把「安装前卸载」放在第一位且**默认选中**（`$ReinstallPageCheck` 初值为 0）。用户手快点了下一步就会先跑卸载程序。
触发背景_动机: 用户明确要求「默认就是直接可以覆盖式更新」，并选了「覆盖前提示一下」——即保留该页但把默认项改对。
决策内容: 用自定义 NSIS 模板（`bundle.windows.nsis.template`）替换默认选中项：升级/降级默认勾「请勿卸载」，**同版本分支保持与上游逐字一致**。提示页保留。
持续副作用: **这份模板从此与上游分叉，官方修复不会自动进来。** 将来同步上游时，**以上游模板原文为基线重贴那段改动**，而不是手改上游文件。差异只有 1 个 hunk、1 个函数（`PageReinstall`），模板里用注释标出了改动边界。
影响范围: `src-tauri/nsis/installer.nsi`、`tauri.conf.json` 的 `template` 字段
后续约束: **不要以「构建产物」为模板基线**——渲染产物里的占位符已被替换成本机绝对路径与当时版本号（实测 6 处绝对路径），提交它等于把构造机的一次性取值写死进仓库。模板必须从上游原文提取，**76 处占位符全部原样保留**，它们由 bundler 的 Handlebars 管线渲染。
回滚或替代: 若上游把「默认直接覆盖」做成配置项，即可删除自定义模板。
证据与来源: `src-tauri/nsis/installer.nsi`、`reinstall-default-selftest.nsi`、`docs/MAINTENANCE-LIABILITIES.md` 的 M-01
关联条目: MD-0001、MD-0003

<!-- END MD-0011 -->

---

## [MD-0012]

编号: MD-0012
写入时间: 2026-09-24 10:40:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 架构·接口
标题: 凭据类与「机器专属」类应显式分开
触发背景: 云同步的排除清单 `is_setting_sync_eligible` 同时承担了两种语义：**「这台机器专属、不该跨机器同步」**（AI 接口配置、自动备份配置、窗口尺寸）与**「凭据、不该离开本机」**。
触发背景_动机: 安全审计发现该清单只列了 5 个敏感键中的 2 个，导致 **MQTT 密码、MQTT 用户名、AI 配置**既会被上传到云端、也会被远端快照写回本机。
决策内容: 已修：在权威函数开头加 `is_sensitive_key(key)` 检查，与加密侧**共用同一个判据**（`SENSITIVE_KEYS` 是「凭据」这件事的唯一定义处）。
持续副作用: 两类语义混在同一个 `matches!` 里，**是这次漏掉 3 个凭据键的结构性原因** —— 前者靠「想起来加一行」，后者现在靠 `is_sensitive_key` 兜住。
影响范围: `src-tauri/src/services/cloud_sync.rs` 的 `is_setting_sync_eligible`、`src-tauri/src/database.rs` 的 `SENSITIVE_KEYS`
后续约束: **不要往 `SENSITIVE_KEYS` 之外再抄一份凭据清单**；新增凭据键只改 `SENSITIVE_KEYS`。测试遍历该数组全成员，加第 6 个键会自动被覆盖。
回滚或替代: 若将来进一步加固，建议把两类语义**在代码里显式分开**（两组注释 + 两条断言），而不是继续往同一个 `matches!` 里加。此项**尚未做**。
证据与来源: `src-tauri/src/services/cloud_sync.rs`、`database.rs`；测试 `credential_keys_never_sync_to_the_cloud`
关联条目: MD-0007

<!-- END MD-0012 -->

---

## [MD-0013]

编号: MD-0013
写入时间: 2026-09-24 10:40:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 架构·接口
标题: 「用了未定义的样式变量」纳入构建拦截
触发背景: 项目里累积了 38 个「被 `var()` 引用但从未定义」的 CSS 变量。浏览器对 `var()` 解析失败是**静默的**：取不到值时整条声明被丢弃，构建、类型检查、运行时不报任何错，界面只是「少了边框/背景/阴影」。
触发背景_动机: 其中一个数字输入框因此**完全没有边框**，而代码看上去完全正常。这类缺陷能长期存在的根本原因就是**没有任何机制会报警**。
决策内容: 全部修复，并把检查接入 `npm run build`（缺失即构建失败）。检查脚本自动识别运行时注入（`setProperty` / React style 的 `["--x"]` 键 / `<style>` 模板串），并区分致命与可豁免两类。
持续副作用: **白名单不能豁免无兜底的用法**（脚本判 `illegalAllow` 失败）；**白名单里已不再被引用的条目会报 `stale` 失败**，避免豁免长期残留。另有一条契约测试保证「检查脚本本身会因缺陷而失败」，防被改成恒真。
影响范围: `tools/check-css-vars.mjs`、`tools/css-vars-allowlist.json`、`package.json` 的 `build` 脚本
后续约束: **`tools/` 必须纳入版本控制** —— `npm run build` 依赖它，不提交会导致他人 checkout 后构建直接失败。其 `dist/` 产物已加入 `.gitignore`。
回滚或替代: 不适用（防护长期保留）。
证据与来源: `tools/check-css-vars.mjs`、`docs/MAINTENANCE-LIABILITIES.md` 的 M-15
关联条目: MD-0001

<!-- END MD-0013 -->

---

## [MD-0014]

编号: MD-0014
写入时间: 2026-09-24 10:40:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 环境适配·方法论
标题: 探针与夹具本身也需要「能失败的证明」
触发背景: 有些逻辑无法单独运行：它写在几百行文件的中间，依赖同文件几十个定义，而整个文件又依赖构建系统注入的占位符（Windows 安装器模板即如此，单独编译直接报缺 `utils.nsh`）。
触发背景_动机: 需要在没有目标平台宿主的机器上验证真实运行时行为。
决策内容: 写脚本**从生产文件原样剪出**目标片段拼进最小可运行外壳，在真实运行时里跑并**读回真实状态**；生成过程写成生成器脚本入库，外壳头部注明「由生成器生成，不可手改」。
持续副作用: **必须是剪出而非手抄**——手抄的副本会与被测代码漂移，验证的就不是同一份逻辑了。
影响范围: `src-tauri/nsis/reinstall-default-selftest.nsi`、`gen-reinstall-probe.py`
后续约束: 必须配**三段自证**，缺一不可：①**反向对照**（换成改动前的实现，目标用例必须变红）；②**脏夹具显形**（未知模式必须判 FAIL）；③**消歧**（分别读两个对象的状态）。**②是真实踩过的坑**：探针第一版六个用例全绿，但日志露出 `MODE==1` —— 命令行解析对 `/MODE=1` 取回的是 `=1` 而非 `1`，六例全落同一分支；没有这条守卫，一个什么都没测到的探针会以 6/6 PASS 交付。
回滚或替代: 不适用（方法论长期有效）。
证据与来源: `src-tauri/nsis/reinstall-default-selftest.nsi`；记忆 `AB2-G3-087`；已反哺 `desktop-release-crossbuild` skill
关联条目: MD-0011

<!-- END MD-0014 -->

---

## [MD-0015]

编号: MD-0015
写入时间: 2026-09-24 11:10:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 接口·安全
标题: 安全告知的送达状态必须只由本机决定
触发背景: 为一次已发生的凭据外流加入升级告知。告知只需显示一次，靠一个标记键记录"已读过"。该标记键最初写成"不在同步白名单里，因此默认不同步"。
触发背景_动机: **这个断言是错的** —— 同步资格函数是**排除法**（末尾 `!matches!`），**未列出的键默认放行**。实测：该标记键会被上传到云端，也能被远端快照改写成 `true`，于是**一条远端快照就能把整条安全告知静默吞掉**。
决策内容: 该标记键所在的 `security.` **整族按前缀排除**同步（不是再补一行逐键），并把该族加进 AI 接口设置写入工具的**拒写闸**。
持续副作用: **任何"这条安全提示是否已展示"的状态，都不得允许外部数据决定。** 否则外部输入（云端快照、AI 接口调用）可以关掉本机的安全提示，而用户毫不知情。两道闸缺一不可：云同步侧（不让它被改写成真）与 AI 接口侧（不让本机 AI 写它）。
影响范围: `src-tauri/src/services/cloud_sync.rs` 的同步资格函数、`src-tauri/src/services/mcp/tools.rs` 与 `tools_extra.rs` 的设置写入闸
后续约束: 新增安全告知类功能时，其"已送达/已确认"标记**必须**落在 `security.` 前缀下（自动获得两道闸）。**不要**把这类标记放到普通设置命名空间里。另：给排除法函数新增键时，**默认是放行**，不是拒绝。
回滚或替代: 不适用（只增加保护）。
证据与来源: `src-tauri/src/services/cloud_sync.rs`、测试 `acknowledgement_key_cannot_be_synced_from_the_cloud`、`set_setting_refuses_security_keys`；记忆 `AB2-G3-089`、`AB2-G3-088`
关联条目: MD-0007、MD-0012

<!-- END MD-0015 -->

---

## [MD-0016]

编号: MD-0016
写入时间: 2026-09-24 13:40:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 数据布局·文件操作
标题: 改名让位类操作必须「失败即回滚」
触发背景: 迁移时给目标空库改名让位（`clipboard.db` → `clipboard.db.unused-<时间戳>`），循环逐条改名：主库 → `-wal` → `-shm`。**主库改名成功、`-wal` 失败**时，旧实现的 `?` 直接返回，**已改名的主库没人放回**。
触发背景_动机: 真机反馈迁移报 `os error 32`（`fs::rename` 改名被应用自己占用的文件）。排查时发现这个次生缺陷：用户会看到「迁移失败」但目标目录里主库已变成 `.unused-`，**数据看起来消失了** —— 比原始报错更糟。
决策内容: 改名循环的**每一条失败路径都必须把本次已改名的条目逐个还原**，并在文案里明确告知「已让位到一半的文件已全部放回原位，目标目录恢复成你操作前的样子」。还原失败的条目**不吞掉**，一并列给用户（比静默留档可诊断）。
持续副作用: 本仓库有两处这样的改名循环（迁移、导入），**契约都是「失败即回到调用前状态」**。新写同类循环时必须遵守；`restore_*` 类还原函数只在**调用方**失败分支被调是不够的 —— 循环自身的失败路径也要还原。
影响范围: `src-tauri/src/migration_identifier.rs` 的 `yield_target_db_with`；参考实现 `src-tauri/src/services/backup/import.rs` 的 `swap_managed_entries`
后续约束: 循环里**不要**写 `op().map_err(...)?` 后就假设不用回滚 —— `?` 会直接跳出函数，把已做的改动留在原地。要么显式处理失败分支，要么用「先全部验证再执行」的两段式。
回滚或替代: 不适用（只增加正确性）。
证据与来源: `src-tauri/src/migration_identifier.rs`、测试 `failure_after_the_main_db_already_moved_puts_it_back`
关联条目: MD-0011

<!-- END MD-0016 -->

---

## [MD-0017]

编号: MD-0017
写入时间: 2026-09-24 13:40:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 兼容约束·文件操作
标题: 「被占用」的判定不能依赖平台错误码
触发背景: 迁移报错原样把系统错误抛给用户：`另一个程序正在使用此文件，进程无法访问。(os error 32)`。
触发背景_动机: 需要把「被占用」识别出来换成可执行的提示。最初的直觉是「判 `raw_os_error() == Some(32)`」。
决策内容: 改用 **`ErrorKind` + 文件名的组合**判定：`PermissionDenied` 或 `Other` 命中即按占用处理。
持续副作用: **实测反例**：Linux 上 `Error::from_raw_os_error(32)` 是 **`BrokenPipe`**，不是 `PermissionDenied` —— 同一个数字在不同平台含义不同。写死 32 在 Linux 上会误判，在 Windows 上才碰巧对。
影响范围: `src-tauri/src/migration_identifier.rs` 的 `occupied_or_failed_hint`
后续约束: 跨平台错误处理**一律走 `ErrorKind`**，只有需要给支持人员看的技术细节才附带 `raw_os_error()`（且要说清那是平台相关值）。
回滚或替代: 不适用。
证据与来源: `src-tauri/src/migration_identifier.rs`
关联条目: MD-0016

<!-- END MD-0017 -->

---

## [MD-0018]

编号: MD-0018
写入时间: 2026-09-24 13:40:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 环境适配·验证方法
标题: 验证台漏加载样式会让「多主题验证」整体失真
触发背景: 内部视觉验证台 `tools/visual-harness` 的 `main.tsx` **漏了 `import themes/load`**（生产 `src/main.tsx` 有这一行）。后果：`theme-mica` / `theme-paper` / `dark-mode` 这些类挂到根元素上**毫无效果**，页面只剩 `base.css` 的默认变量。
触发背景_动机: 为修「备份列表」弹窗的对比度问题做跨主题量测时发现：`--bg-button` 在六套主题下**全部返回同一个 base 值** `rgba(255,255,255,.76)`。
决策内容: 补齐该 import，并在注释里写明生产 `main.tsx` 的样式加载顺序（`index.css` → `styles/components/index.css` → `styles/themes/load`），要求验证台与之逐字一致。
持续副作用: **此前的「6 主题 × 明暗」量测全部不可信** —— 它不是「量到相同值」，而是「根本没加载主题」。修复后立刻暴露出修前基线里更严重的问题（浅色主题下底部提示对比度最低只有 2.20，而按假基线看到的差异小得多）。
影响范围: `tools/visual-harness/src/main.tsx`
后续约束: 验证台**各文件顶部必须写明它依赖哪些全局样式**。改完验证台后，**先用一个已知会随主题变化的变量做自检**（如 `--bg-button` 在不同主题下必须不同），确认主题真的生效，再开始正式量测。
回滚或替代: 不适用（方法论长期有效）。
证据与来源: `tools/visual-harness/src/main.tsx`、`tools/visual-harness/README.md`
关联条目: MD-0013

<!-- END MD-0018 -->

---

## [MD-0019]

编号: MD-0019
写入时间: 2026-09-24 16:32:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 执行流程·验证方法
标题: 改名让位与覆盖类操作的文件句柄约束
触发背景: 迁移要给目标数据库改名让位，但应用启动时就打开了它（连接常驻），Windows 不允许改名已打开的文件 —— 报 `os error 32`。
触发背景_动机: 真机反馈迁移不可用，且提示让用户「完全退出应用后重试」——但退出后就点不到应用内的迁移按钮了，这条提示不可执行。
决策内容: 改为**两阶段**：运行期只把源复制到暂存并写「待接管」标记；**下次启动、打开数据库之前**做改名交换（此时无人持句柄）。注入点三条约束：开库前 / 数据目录解析后 / logger 初始化后。失败只记日志、保留标记，不阻断启动。
持续副作用: 「运行期热替换数据库连接」已被证不可行：`app.manage` 对已存在类型丢弃新值并 panic，且连接 Arc 被多处持有（state + 各 repo + MCP），计数不可能归零。**重启是唯一正解**，不要再尝试绕。
影响范围: `src-tauri/src/migration_identifier.rs`、`src-tauri/src/migration_pending.rs`、`src-tauri/src/app/setup.rs`
后续约束: 今后凡是「入口在应用内」且「要改的文件正被自己占用」的操作，先判定是否死循环（三条件），再决定两阶段；**不要**写出「请先退出应用再点我」这种不可执行的提示。
回滚或替代: 不适用（是能力修复）。
证据与来源: `src-tauri/src/migration_pending.rs`、测试 `occupied_or_failed_hint` 与启动顺序守卫
关联条目: MD-0016

<!-- END MD-0019 -->

---

## [MD-0020]

编号: MD-0020
写入时间: 2026-09-24 16:32:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 执行流程·验证方法
标题: 平台错误码判定必须走 ErrorKind，且留真机路径的测试
触发背景: v0.5.2 用 `matches!(e.kind(), PermissionDenied | Other)` 判定「文件被占用」。真机上完全没命中，用户仍看到原始系统错误。
触发背景_动机: 要让「被占用」提示可执行，就需要准确识别它。
决策内容: 改为 **`raw_os_error()` 的平台错误码 + `ErrorKind` 组合**，并用 `cfg(windows)` 隔离（Windows 认 32/33）。测试夹具改用 `io::Error::from_raw_os_error(32)`。
持续副作用: **这条判定原本是死代码**：`ErrorKind::Other` 在 `core/src/io/error.rs` 里明确写着「不被标准库使用」；Windows 的 `ERROR_SHARING_VIOLATION`(32) 也不在 kind 映射表里，落 `Uncategorized`。而**测试夹具人工构造了 `PermissionDenied`**，于是测试通过、真机必然失败。
影响范围: `src-tauri/src/migration_identifier.rs` 的 `looks_occupied`
后续约束: **涉及平台错误的测试一律用 `from_raw_os_error(<真实码>)`**，禁止人工构造 `ErrorKind`。并且注意 `ErrorKind::Uncategorized` 是 **unstable**，stable 工具链上**无法 match**（实测 `error[E0658]`）。
回滚或替代: 不适用。
证据与来源: `src-tauri/src/migration_identifier.rs`、测试 `windows_sharing_violation_is_recognized_as_occupied`
关联条目: MD-0019

<!-- END MD-0020 -->

---

## [MD-0021]

编号: MD-0021
写入时间: 2026-09-24 16:32:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 环境适配·验证方法
标题: 非目标平台上验证会产生三种假绿
触发背景: 在 Linux 上构建并测试 Windows 目标产物时，出现了三类「测试全绿但零覆盖」的情况。
触发背景_动机: 交叉编译场景下，测试跑在目标平台，编译期与运行期语义都与宿主不同。
决策内容: 识别并规避三种形态：① **`cfg` 把断言整段编译掉**（`#[cfg(unix)]` 断言在 Windows 目标上不存在）；② **正则式门禁的注释误报**（解释规则的注释被当成使用点，报错指向注释自己）；③ **存在性检查判别力不足**（「文件里包含键名」被同文件的读取点满足）。
持续副作用: 三者都会让「改坏了也不报错」。① 的修法是改用跨平台稳定判据（如 `Metadata::modified()`）；② 的修法是**只剥注释、不剥字符串**（内联样式里的 `var()` 是真实用法）；③ 的修法是收紧到**同一行/同一语法位置**。
影响范围: `tools/check-css-vars.mjs`（已修 ②）、`src/shared/lib/*.test.ts`（③ 的判据写法）
后续约束: 每条「存在性」断言自问：**删掉它要验的那个东西，这条还绿吗？** 还绿就是没有判别力。改断言后必须做**双向**验证（误报已修 + 真缺陷仍被抓 + 没矫枉过正）。
回滚或替代: 不适用（方法论长期有效）。
证据与来源: `tools/check-css-vars.mjs`、候选记忆 `AB2-V2-009`；已反哺 skill `desktop-release-crossbuild` 的 3.9–3.11
关联条目: MD-0018

<!-- END MD-0021 -->

---

## [MD-0022]

编号: MD-0022
写入时间: 2026-09-24 16:32:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 兼容约束·配置默认值
标题: 出厂默认值只能有一处权威，且必须与读取端判据一致
触发背景: `app.persistent`（剪贴板历史是否落盘）的三处默认值互相矛盾：种子写 `'false'`（关），后端读取兜底 `.unwrap_or(true)`（开），前端 `!== "false"`（键缺失即开）。
触发背景_动机: 用户反馈「默认：持久化开关是开启的」，与代码里的种子值相反。
决策内容: **统一为「开」**：把种子改成 `'true'`（另两处本来就是开，判据不动）。同理统一 `app.theme`（种子 `mica` vs 兜底 `retro`）。
持续副作用: 由于种子**一定会写入**，读取端的兜底**永远不被触发** —— 于是「代码写着开、新库实际是关」；只有旧库/导入包（键缺失）才走兜底拿到「开」。用户看到的正是后者。**三处方向相反时，真实行为取决于哪一处先落地，极难排查。**
影响范围: `src-tauri/src/database.rs`、`src-tauri/src/app/setup.rs`、`src/shared/hooks/useSettingsPostInit.ts`
后续约束: 新增设置键时，**种子值与所有读取端的兜底必须一次性对齐**，并留一条测试跑真实种子再读回（不要只断言 SQL 字符串）。
回滚或替代: 不适用。
证据与来源: `src-tauri/src/database.rs`、测试 `persistent_default_is_on_and_matches_readers`
关联条目: MD-0017

<!-- END MD-0022 -->

---

## [MD-0023]

编号: MD-0023
写入时间: 2026-09-24 16:32:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 执行流程·变更管理
标题: 反向对照的回滚步骤本身是最危险的一步
触发背景: 为验证修复有效，采用「改坏 → 确认变红 → 回滚」的反向对照。回滚时误用 `git checkout -- <文件>`。
触发背景_动机: 该命令把文件的**全部未提交状态**恢复到 HEAD，于是同时抹掉了**另一个并行代理**新增的词条与自己的新增内容。两边都未提交，**无 stash / reflog 可救**。
决策内容: 最终从**早于事故的构建产物**逐键提取三语原文恢复。恢复后经四层核验（三语键数、内容逐字、占位符一致性、结构配平）。
持续副作用: **危险的不是「改坏」，是「回滚」**：改坏最多让一条断言报错、损失可见；**回滚改错会丢掉未入库的全部工作，且在多代理共享工作区会跨代理破坏别人的成果**。靠构建产物恢复是运气，不是流程。
影响范围: `src/locales.ts`（事故与恢复）、候选记忆 `AB2-V2-010`
后续约束: **禁用 `git checkout -- <文件>` 做回滚**。变异前先备份并**校验**（前置条件是「备份存在且校验通过」，不是「我复制了」）；回滚用**精确反向编辑**；回滚后 diff 确认只回到变异前。任何**覆盖整个文件**的操作（`git checkout --`、`git restore`、`> file`、批量格式化）在共享工作区都应视为**破坏性操作**。
回滚或替代: 不适用。
证据与来源: `src/locales.ts`；已反哺 skill `desktop-release-crossbuild` 的 3.12
关联条目: MD-0005

<!-- END MD-0023 -->

---

## [MD-0024]

编号: MD-0024
写入时间: 2026-09-24 17:20:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 对外文案·发布流程
标题: 发布说明面向用户，与 README 的分工是单向派生
触发背景: 用户读 v0.5.3 的 Release notes 后反馈："不要这么开发消息""不要讲这么和我说话一样，他是面对用户的"。核查发现 v0.5.1–v0.5.3 每篇都带「后端测试 N passed / 构建 exit 0」验证表格，v0.5.3 还有 `ErrorKind::Other`、`ERROR_SHARING_VIOLATION(32)`、第一人称"我用来识别"、以及 `### 请你验证` 这类对话式祈使句。
触发背景_动机: 发布说明是长期对外可见的，新用户会翻历史版本判断"这版修了什么"。混入开发过程会让用户读到大量与自己无关的内容，也会把"该做什么"淹没掉。
决策内容: 发布说明与 README 的分工定为**单向派生**：GitHub Releases 是完整权威版本（面向用户），README「版本记录」只写摘要 + 链接，**不复制正文**。发布说明按三条判据书写：① **用户可见性**（只写用户能在界面或报错里看到的东西）；② **可行动性**（说清"我要做什么"或"我什么都不用做"）；③ **责任归属**（不用"我/我们"指代开发方）。每篇至少含 `### 安装` 与 `### 说明` 两节。
持续副作用: 今后每篇发布说明都要过这三条判据；README 版本记录不再维护完整正文，只保留摘要与链接；`releases/` 目录成为原文存档与体例范本；改写已发布说明前必须先在该目录留底。
影响范围: `README.md`（版本记录 + 维护约定第 6 条）、`releases/`（新建，含 5 篇原文存档与 README）、后续所有 Release notes
后续约束: **判据不是"看起来技术"，而是"用户能否在自己的屏幕上看到它"。** 因此 `os error 32` 这类**用户会实际看到的报错原文必须保留**（`README.md` 的迁移说明里有引用），而 `ErrorKind::Other`（用户永远看不到）必须删除。这两者性质完全不同，不要一起清理。每篇 Release 必须写明下载哪个文件 —— 每篇下有 `setup.exe` 与 `sig` 两个附件，v0.5.0–v0.5.3 都漏了这节，用户不知道该下哪个。v0.3.4 是体例范本（四项违规扫描零命中），恢复它的骨架即可，不必另创规范。
回滚或替代: 若要恢复"README 写完整正文"，需同时解决"同源双写"（实测 v0.5.3 的 README 段与 Release body 实质 100% 重叠），否则会重现改一处忘一处。原文存档在 `releases/`，任何改写都可从那里取回。
证据与来源: `releases/v0.5.0.md`~`v0.5.3.md`（改写后的对外版本）、`releases/v0.3.4.md`（范本）、`README.md` 的「维护约定」第 6 条、`docs/MAINTENANCE-LIABILITIES.md:26-33`（三文档分工表）
关联条目: MD-0001（发布链路重建）

<!-- END MD-0024 -->

---

## [MD-0025]

编号: MD-0025
写入时间: 2026-09-24 18:30:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 环境适配·验证方法
标题: 大容器上的 backdrop-filter 会整片压暗内容，与主题无关
触发背景: 用户反馈「mcp 那个压暗还是有，但是好很多了」——上一轮已修过弹窗透明度与遮罩，仍有残余。
触发背景_动机: 「压暗」看代码看不出来，是多机制叠加的结果；上一轮曾误判为分组标题色带，方向完全错。
决策内容: 用真实组件量测（挂真实 `McpSettingsGroup` + 读 computed style）定位到两处：① `settings.css` 的 `.settings-group` 上有 `backdrop-filter: saturate(120%)`；② `retro.css` 的 `--data-panel-shadow` 是 `inset 3px 3px 0 rgba(0,0,0,0.05)`（其余五个主题均为 `none`）。两处均已删除。
持续副作用: **整页容器上不得使用 `backdrop-filter`** —— 它处理的是元素**背后**的内容，作用在整页大的容器上会把背后的窗口底色整体重新调色。`backdrop-filter` 只用于小面积元素（弹窗、浮层、输入框），那些是有意的玻璃感。
影响范围: `src/styles/components/settings.css`（`.settings-group`）、`src/styles/themes/retro.css`（`--data-panel-shadow`）
后续约束: **`.settings-group` 规则体内不得再出现 `backdrop-filter`**（规则体内已留长注释说明原因）。排查"某块发暗/发灰"的顺序：先看 `backdrop-filter` → 再看 `filter`/`opacity` → 再看 `box-shadow: inset` → **最后**才怀疑背景色。反直觉证据：retro 的 `--bg-panel` 是**实心** `#f3f3f3`，本不该有透色却仍发闷 —— 说明问题不在背景色。
回滚或替代: 若要恢复玻璃感，只能在**小面积**元素上做，不能回到整页容器。原文与判据见 `plans/v0.5.4-ux-and-docs/plan.md` 的 B 项章节。
证据与来源: `tools/visual-harness/src/mcpdim.tsx` + `mcpdim-probe.mjs`（量测台）；改前改后六主题 computed `backdropFilter` 数值；像素 diff 2.23%；视觉模型复核
关联条目: MD-0024

<!-- END MD-0025 -->

---

## [MD-0026]

编号: MD-0026
写入时间: 2026-09-24 18:32:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 接口·兼容约束
标题: 备注可编辑性按「正文可编辑类型的补集」判定
触发背景: 用户要求「备注每一个条目都要支持编辑备注」。量测发现 `emoji_sync` 与未知类型**没有备注入口**。
触发背景_动机: 原判据是 `BINARY_CONTENT_TYPES.includes(contentType)` —— 一个只含 `["image","file","video"]` 的白名单，凡不在其中的类型都拿不到备注入口。
决策内容: 改为 `!EDITABLE_BODY_TYPES.includes(contentType)`，即「除已知可编辑正文的类型外，一律可编辑备注」。
持续副作用: 后端将来**新增任何内容类型**都会自动获得备注入口，不再需要同步改前端白名单。备注写路径是唯一的 `update_entry_note`，与内容类型无关，因此不存在"未审查的写路径"。
影响范围: `src/features/clipboard/types.ts` 的 `isNoteEditable`
后续约束: **`isBodyEditable` 仍是白名单，不要一并改成补集** —— 正文编辑确实存在"未预见的类型会静默获得未经审查的写路径"的风险，`isNoteEditable` 则不存在。两者判据方向不同是**有意的**，不是不一致。新增内容类型时只需确认它属于哪一类正文，不必再动备注判据。
回滚或替代: 若某类型确需禁止备注，应显式加黑名单并说明理由，而不是退回白名单（退回会让新类型再次静默失去入口）。
证据与来源: `tools/visual-harness/collapse.mjs` 量测（改前 `emoji_sync`/`unknown_type` 的 noteEdit=false，改后 true）；反向对照退回白名单后 12 条断言变红
关联条目: MD-0025

<!-- END MD-0026 -->

---

## [MD-0027]

编号: MD-0027
写入时间: 2026-09-24 18:34:00 +08:00
发生时间: 2026-09-24
状态: 生效
类别: 执行流程·验证方法
标题: 系统级设置清单不得用「已读标记」，探测失败必须与未满足区分
触发背景: 用户要求「有需要用户介入帮忙的列成清单逐条让用户帮忙，用户点击一条回复、设置一条……所以纯应用里面显示设置了不一定生效，要用户手动一条一条开起来，就像安卓应用申请权限一样」。
触发背景_动机: 应用无法代做的系统项只有三项（防火墙放行、UAC 提权、任务管理器禁用启动项）。同类组件（凭据暴露告知）用的是一次性"已展示并处理过"标记，本清单不能照抄。
决策内容: 新增 `system_checklist` 模块：探测与判定分离（判定为纯函数，可在非 Windows 上单测）；每轮实时探测；`ack`（不再提醒）只在用户显式点击时写入 `security.system_setting_<id>_ack`，且**不影响**探测结果；探测失败是独立的第三种状态 `probe_ok: false`，界面用虚线边与中性色呈现，且**不提供**「不再提醒」按钮。
持续副作用: 三项会长期出现在设置页的「数据与迁移」里（已满足的自动隐藏）。用户若在任务管理器里又禁用了启动项，下次打开设置页仍会看到它 —— 这是**有意**的，不是重复打扰。
影响范围: `src-tauri/src/app/commands/system_checklist.rs`、`src/features/settings/components/SystemChecklistPanel.tsx`、`DataSettingsGroup.tsx`、`settings.css`
后续约束: **不得**给探测失败项加「不再提醒」—— 认知上无法确认的事不该被掩盖。**不得**让 `ack` 改变 `satisfied`（那正是"上次点了知道了，这次真坏了却不提示"）。非 Windows 上三项探测必须返回 `Err`（探测不可用），**不得**返回 `Ok(None)`（会被显示成"待处理"，让用户去做一件做不到的事）。
回滚或替代: 若要减少打扰，只能让已满足项隐藏（已实现），不能引入"已读"语义。
证据与来源: `src-tauri/src/app/commands/system_checklist.rs` 的 5 条测试；反向对照 RC-1（探测失败与未满足混同）与 RC-2（ack 影响 satisfied）各使断言变红
关联条目: MD-0026

<!-- END MD-0027 -->